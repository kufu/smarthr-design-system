---
title: 'コンポーネントの実装・運用'
description: ''
order: 1
---

## はじめに

- SmartHR UIの実装と運用の規約・方針をまとめたものです。
- 分割単位や命名規則、スタイルの書き方などを記載しています。
- コンポーネントの品質を高めると同時に、開発の敷居を下げることを目的としています。

## 対象

https://github.com/kufu/smarthr-ui

## ディレクトリ構成

`src` 以下のディクトリ構成について説明します。

- components
    - React コンポーネントを入れるディレクトリです
    - この中は export するコンポーネントの粒度でディレクトリを切ります
- hooks
    - React Hooks な関数を記述する場所です
- libs
    - 文字列操作みたいな汎用的な関数を記述する場所です
- themes
    - テーマの設計については後述します
- types
    - グローバルな型定義をする場所です


## 設計の基本方針

Atomic Design などのような難しい設計はしておらず、単純に SmartHR UI のユーザーが import するコンポーネントの単位がディレクトリの分割単位です(例外あり)。

例外として、 例えば Button というディレクトリからは PrimaryButton, SecondaryButton, TextButton といったようなテーマごとの Button コンポーネントを export していたりします。
コンポーネントの内部の記述として、基本的には難しい処理を持たせるべきではないですが、どうしても必要になる場合は hooks を使用することを推奨しています。

## コーディング規約

### 分割単位

SmartHR UIのユーザーがimportするコンポーネントの単位がディレクトリの分割単位です。 
UIパーツの単位で分割しています。

### コンポーネントの命名

準備中

### ファイルの命名

準備中

### Propsの命名

準備中

### その他

- コンポーネントの内部の記述として、基本的には難しい処理を持たせるべきではないと考えていますが、必要になる場合は hooks の使用を推奨しています。
