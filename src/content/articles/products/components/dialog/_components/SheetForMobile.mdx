デスクトップに比べて、モバイルでは表示できる情報量に限りがあります。

シート形式のダイアログを利用することで、モバイルの表示領域を最大限活用して情報を表示できます。

例えば、次のような場合、シート形式が特に有効です。

- ダイアログが画面を占める割合が50％を超える場合
- 入力や編集など、キーボードでの入力操作が多い場合

シート形式のダイアログを利用することで、モバイルの表示領域を最大限活用して情報を表示できます。
