---
title: '余白'
description: '余白トークンは、主にレイアウトで使われるトークンです。'
order: 3
---
余白トークンは、主にレイアウトで使われるトークンです。

CSSでは`margin`や`padding`, `top`, `left`, `bottom`, `right`が関連します。

## 基準値
ベースの基準サイズは`16px`です。

## プリミティブトークン

| トークン名 | 値（rem） | 値（px） | 説明 | 他のトークンとの使い分け | 注意事項 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `0`    | 0       | 0    | - | - | - |
| `0.25` | 0.25rem | 4px  | `StatusLabel`やアイコンなどの要素をテキストと組み合わせる場合<ul><li><code>InformationPanel</code>や<code>FormControl</code>、<code>FieldSet</code>、<code>TextLink</code>を利用することで、自動的に適用されます。その他の場合は、<code>Cluster</code>を使用して余白を指定してください。</li></ul> | <ul><li><strong>0.5（8px）</strong>: StatusLabelやアイコンなどの要素をテキストと組み合わせる場合</li></ul> | StatusLabelやアイコンなどの要素をテキストと組み合わせる場合、明確な理由がない場合、原則として**0.5（8px）**を利用します。 |
| `0.5`  | 0.5rem  | 8px  | **0.5（8px）**: StatusLabelやアイコンなどの要素をテキストと組み合わせる場合 | <ul><li><strong>0.25（4px）</strong>: StatusLabelやアイコンなどの要素をテキストと組み合わせる場合</li><li><strong>1（16px）</strong>:<ul><li>ブロックの見出しやInputにラベルや説明テキストを複合的に組み合わせる場合</li><li>Button同士の間</li></ul></li></ul> | <ul><li>StatusLabelやアイコンなどの要素をテキストと組み合わせる場合、明確な理由がない限り、原則として<strong>0.5（8px）</strong>を利用します。</li><li>Button同士の間のmarginを指定する際、原則として<strong>1（16px）</strong>を利用します。</li></ul> |
| `0.75` | 0.75rem | 12px |  - | - | **[モバイル]** 0.5（8px）では余白が小さく、1（16px）では余白が大きい場合に0.75（12px）を利用する。 |
| `1 `   | 1rem    | 16px | <ul><li>セクション内の見出しとコンテンツの間</li><li>フォーム要素内の説明テキストとフォームの間</li><li>各セクションの見出しとコンテンツの間</li><li><code>UpwardLink</code>と画面タイトルの間</li><li>ブロックの見出しや<code>Input</code>にラベルや説明テキストを複合的に組み合わせる場合<ul><li><code>FormControl</code>や<code>Fieldset</code>を利用することで、自動的に適用されます。</li></ul></li><li><code>Checkbox</code>または<code>RadioButton</code>の<code>row-gap</code></li><li>フォームを横並びにする際の<code>row-gap</code></li><li><code>Button</code>同士の間<ul><li>0.5（8px）のトークンもあります。</li></ul></li><li>代表的なページ内レイアウトのパディング<ul><li>[モバイル] メインコンテンツのパディング：<code>Container</code>を利用することで、自動的に適用されます。<code>padding</code> propsで値を指定できます。<code>UpwardLink</code>がある場合は、<code>UpwardLink</code>がある場合のパディングを参照して、<code>Container</code>の<code>padding</code> propsを指定してください。</li><li>[モバイル] コンテンツエリアのパディング：<code>Base</code>の<code>padding</code> propsで値を指定してください。</li><li>コンテンツエリア内のグループのパディング：<code>BaseColumn</code>を利用することで、自動的に適用されます。</li></ul></li><li>代表的なダイアログ内レイアウトのパディング<ul><li>[モバイル] コンテンツエリアのパディング：<code>Dialog</code>の各種コンポーネントを利用することで、自動的に適用されます。値は<code>contentPadding</code> propsで指定できます。</li><li>コンテンツエリア内のグループのパディング：<code>BaseColumn</code>を利用することで、自動的に適用されます。</li></ul></li><li>共通<code>Table</code>においてオブジェクトの数が0のとき、メッセージ下端と<code>Button</code>の距離</li><li>共通<code>List</code>においてオブジェクトの数が0のとき、メッセージ下端と<code>Button</code>の距離</li><li><code>Table</code>の<code>TitleArea</code>とテーブルの上端の間のマージン</li></ul> | <ul><li><strong>0.5（8px）</strong><ul><li>Button同士の間</li></ul></li><li><strong>1.5（24px）</strong><ul><li><strong>[モバイル]</strong> 代表的なページ内レイアウトのメインコンテンツのパディング</li></ul></li></ul> | <ul><li>Button同士の間のmarginを指定する際、原則として<strong>1（16px）</strong>を利用します。</li></ul> |
| `1.25` | 1.25rem | 20px | - | - | - |
| `1.5`  | 1.5rem  | 24px | <ul><li>代表的なページ内レイアウトのマージン<ul><li>(必要に応じて) セクション同士の間<ul><li>視覚的に余白が大きく見える場合のマージン</li></ul></li><li>セクション内の要素同士の間</li></ul></li><li>代表的なダイアログ内レイアウトのマージン<ul><li>フォーム要素同士の間</li></ul></li><li>タイトルエリアに <code>InformationPanel</code> を配置しない場合のタイトルエリアと最初のセクションの間</li><li><code>Checkbox</code> や <code>RadioButton</code> の <code>column-gap</code><ul><li><code>Cluster</code> を使用して余白を指定してください</li></ul></li><li>[モバイル]メインコンテンツのパディング<ul><li><code>Container</code> を使用することで、自動的に適用されます。<code>padding</code> propsで値を指定できます。<code>UpwardLink</code> がある場合は、<code>UpwardLink</code> がある場合のパディングを参照して <code>Container</code> の <code>padding</code> propsを指定してください。</li></ul></li><li>コンテンツエリアのパディング<ul><li><code>Base</code> の <code>padding</code> propsに値を指定してください。</li></ul></li><li>代表的なダイアログ内レイアウトのパディング<ul><li>コンテンツエリアのパディング<ul><li><code>Dialog</code> の各コンポーネントを使用することで、自動的に適用されます。<code>contentPadding</code> propsで値を指定できます。</li></ul></li></ul></li><li>[モバイル] <code>UpwardLink</code> がある場合の <code>padding-top</code><ul><li>代表的なページ内レイアウトのパディング</li></ul></li><li><code>Table</code> の下部と <code>Pagination</code> のマージン</li></ul> | <ul><li><strong>1（16px）</strong><ul><li>メインコンテンツのパディング</li></ul></li><li><strong>2（32px）</strong><ul><li>セクション同士の間</li></ul></li></ul> | セクション同士の `margin` を指定する際、原則として**2（32px）**を利用 |
| `2`    | 2rem    | 32px | <ul><li><strong>セクション同士の間</strong><ul><li>必要に応じて1.5（24px）（視覚的に余白が大きく見える場合のマージンを参照）</li></ul></li><li><strong>メインコンテンツのパディング</strong><ul><li><code>Container</code>を使用することで、自動的に適用されます。<code>padding</code> propsで値を指定できます。<code>UpwardLink</code>がある場合は、<code>UpwardLink</code>がある場合のパディングを参照して<code>Container</code>の<code>padding</code> propsを指定してください。</li></ul></li><li><strong>代表的なダイアログ内レイアウトのマージン</strong><ul><li>説明テキストとフォーム要素のセクションの間</li></ul></li><li><strong><code>UpwardLink</code>がある場合の<code>padding-top</code></strong><ul><li>代表的なページ内レイアウトのパディング</li></ul></li></ul> | **1.5（24px）**: セクション同士の間 | セクション同士のmarginを指定する際、原則として2（32px）を利用 |
| `2.5`  | 2.5rem  | 40px | - | - | - |
| `3`    | 3rem    | 48px | <ul><li>よくあるリストのオブジェクトの数が0件の場合、<code>Base</code>の<code>Top</code>からテキストまでの距離</li><li>よくあるリストのオブジェクトの数が0件の場合、<code>Base</code>の<code>Bottom</code>から<code>Button</code>までの距離</li><li>よくあるリストのオブジェクトの検索結果や絞り込み結果が0件であった場合、<code>Base</code>の<code>Top</code>と<code>Bottom</code>からテキストまでの距離</li></ul> | - | - |
| `4`    | 4rem    | 64px | <ul><li>よくあるテーブルのオブジェクトの数が0件の場合、<code>Base</code>の<code>Top</code>からテキストまでの距離</li><li>よくあるテーブルのオブジェクトの数が0件の場合、<code>Base</code>の<code>Bottom</code>から<code>Button</code>までの距離</li><li>よくあるテーブルのオブジェクトの検索結果や絞り込み結果が0件であった場合がない場合、<code>Base</code>の<code>Top</code>と<code>Bottom</code>からテキストまでの距離</li></ul> | - | - |

## セマンティックトークン

| トークン名 | 値（rem） | 値（px） |
| :--- | :--- | :--- |
| `X3S`  | 0.25rem | 4px  |
| `XXS`  | 0.5rem  | 8px  |
| `XS`   | 1rem    | 16px |
| `S`    | 1.5rem  | 24px |
| `M`    | 2rem    | 32px |
| `L`    | 2.5rem  | 40px |
| `XL`   | 3rem    | 48px |
| `XXL`  | 4rem    | 64px |
| `NONE` | 0       | 0    |

## ［WIP］コード
