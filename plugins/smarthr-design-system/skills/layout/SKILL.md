---
name: layout
description: "smarthr-ui の ColorBox を使うとき、props を選ぶとき、関連するアクセシビリティ・デザインシステムのルールを確認するとき、コンポーネントの組み合わせを判断するときに使う。レイアウトのためのコンポーネントです。Every Layoutを参考に作成しています。"
metadata:
  version: "1.0.0"
  source: smarthr-design-system
  generated-from: layer1
---

レイアウトのためのコンポーネントです。Every Layoutを参考に作成しています。

レイアウトのためのコンポーネントです。[Every Layout](https://every-layout.dev/)を参考に作成しています。

## import

```ts
import { ColorBox } from 'smarthr-ui'
```

## Props

| Props 名 | 型 | デフォルト値 | 必須 | 説明 |
|---|---|---|---|---|
| narrowWidth | boolean | - | - | - |
| narrowHeight | boolean | - | - | - |

## 実装ルール

Layout に直接関連する eslint-plugin-smarthr のルールは現時点ではありません。

## 使い方チェックリスト

checklist.yaml は未作成です。Layer 3（使い方チェックリスト）は今後追加されます。
